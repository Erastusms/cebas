namespace CEBAS.Domain.Entities;

/// <summary>
/// User theme preference setting supporting Light, Dark, and System (OS-preference matching).
/// </summary>
public enum ThemePreference
{
    SYSTEM = 0,
    LIGHT = 1,
    DARK = 2
}
